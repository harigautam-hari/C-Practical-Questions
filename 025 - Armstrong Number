#include<stdio.h>
int main(){
	int n,a,b,c,sum = 0;
	printf("Enter a Number :- ");
	scanf("%d",&n);
	c = n;
	while(n != 0){
		a = n%10;
		b = (a*a*a);
		sum = sum + b;
		n = n/10;
	}
	if( sum == c){
		printf("Armstrong");
	}
	else{
		printf("Not %d",sum);
	}
	
	return 0;
}
