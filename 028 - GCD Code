#include<stdio.h>
int main(){
    int i, a = 10, b = 15, gcd;

    for(i = 1; i<a && i<b; i++){
        if(a % i == 0 && b % i == 0){
        	printf("%d is Common Divisor\n", i);
            gcd = i;
        }
    }

    printf("Greatest Common Divisor = %d", gcd);

    return 0;
}
